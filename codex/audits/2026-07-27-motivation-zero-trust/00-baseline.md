# Zero-trust аудит мотивационного калькулятора — исходное состояние

Дата фиксации: 2026-07-27  
Рабочее дерево: `C:\LocalCalc2\Dom2`  
Проверяемая реализация: `C:\LocalCalc2\Dom2\pub\domian-calculator-a4`  
Статус: Фаза A, до исправлений.

## Выполненные команды этапа 0

### `git status`

- Ветка: `main`.
- Локальная ветка опережает `origin/main` на 3 коммита.
- Изменения не проиндексированы.
- Git показывает 42 удаления отслеживаемых файлов.
- Git показывает новые неотслеживаемые пути:
  - `.gitattributes`;
  - `codex/`;
  - `old/`;
  - `pub/`.
- Активная проверяемая реализация `pub/domian-calculator-a4` целиком не отслеживается Git.

### `git diff --stat`

Фактический итог:

```text
42 files changed, 18564 deletions(-)
```

Удаления охватывают прежний `domian-calculator-a4`, архивные копии, документы планов и корневые HTML-файлы.

### `git diff`

Команда выполнена успешно, код возврата `0`.

Фактический вывод содержит 18 815 строк / около 185 002 токенов и представляет удаления отслеживаемого прежнего дерева. Интерфейс вывода инструмента обрезал отображение длинного результата; никаких изменений из diff не отменялось.

### `git branch --show-current`

```text
main
```

### `git log --oneline -5`

```text
4b0b551 docs: define A4 motivation alert polish
9cf4b38 docs: refine isolated travel implementation
7b813b3 docs: define isolated travel motivation design
53a27b6 456
6f217a7 proof
```

## Классификация исходных изменений

### Изменённые/удалённые отслеживаемые файлы

Git показывает только удаления: 42 файла, включая прежнюю реализацию `domian-calculator-a4`, архивные реализации, планы и корневые HTML-файлы.

### Новые файлы активного проекта

В `pub/domian-calculator-a4` обнаружено 28 неотслеживаемых файлов:

- 8 HTML-файлов;
- 6 CSS-файлов;
- 12 JS-файлов;
- 2 тестовых файла.

### Изменения, не относящиеся к текущему аудиту

Удаления архивов, старых планов, корневого калькулятора пени, прежней копии проекта и реорганизация каталогов предшествуют текущему аудиту. Их происхождение не установлено. Они считаются пользовательскими изменениями и исключены из области автоматического исправления.

### Незакоммиченные пользовательские данные

Рабочее дерево содержит значительный объём незакоммиченных изменений неизвестного происхождения. Активный проект также не отслеживается Git. До установления происхождения нельзя безопасно считать файлы аудита единственными локальными изменениями, выполнять reset/restore либо массово заменять дерево.

Данные браузерного `localStorage` не являются файлами Git и на этом этапе не изменялись.

## Нормативный документ

Источник: `C:\Users\domia\Downloads\Обновленная мотивация карьерного роста (4).docx`.

Пакетный рендерер DOCX не смог запуститься из-за отсутствующего LibreOffice (`WinError 2`). Содержимое документа извлечено структурно через `python-docx`: все непустые абзацы и все строки 8 таблиц. Визуальная пагинация документа на этом этапе не подтверждена.

Ключевые извлечённые правила:

- партнёрские бонусы требуют не менее 250 000 ₽ задатков за квартал;
- пакетные минимумы: Новичок 30%, Стандарт 45%, Расширенный 50%, Продвинутый 55%, Премиум 60%, Премиум+ 65%, Индивидуальный 70%;
- стипендия 3 000 / 4 000 / 5 000 / 6 000 / 7 000 ₽ соответствует квартальному результату 600 000 / 800 000 / 1 000 000 / 1 200 000 / 1 500 000 ₽ и является ежемесячной выплатой в следующем квартале;
- Индивидуальная реклама: 3% от задатков предыдущего месяца, максимум 15 000 ₽;
- реферальная выплата: 2,5% от комиссионного оборота реферала;
- таблица плательщиков мотиваций соответствует матрице, явно заданной пользователем.

## Ограничение исходной точки

Этот файл является фиксацией этапа 0, а не заявлением о корректности реализации. Ни один сценарий интерфейса или расчёт на момент создания baseline не получает статус PASS.
