# Дефекты первичного zero-trust аудита

Документ ведётся до любых исправлений production-кода. Статус `Повторная проверка` заполняется только после отдельного исправления и повторного пользовательского сценария с чистого состояния.

## MOT-QA-001

ID: MOT-QA-001  
Серьёзность: CRITICAL  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: сохранение черновика / переключение режимов  
Предусловия: чистый браузерный контекст; пустой `localStorage`.  
Шаги воспроизведения:
1. Открыть обычный A4-калькулятор.
2. Через интерфейс ввести имя `СТАРЫЙ-A4`, сделку 100 000 ₽ и нажать «Сохранить».
3. Перейти по ссылке в калькулятор мотивации.
4. Через интерфейс изменить имя на `МОТИВАЦИЯ-ПЕРЕЗАПИСАЛА`, выбрать «Премиум» и нажать «Сохранить».
5. Вернуться по ссылке в обычный A4-калькулятор.

Ожидаемый результат: режим мотивации хранит отдельный черновик и не изменяет данные обычного A4.  
Фактический результат: мотивационный режим сразу загрузил A4-черновик, а после сохранения перезаписал его; обычный A4 показал имя `МОТИВАЦИЯ-ПЕРЕЗАПИСАЛА`. Пассивное чтение хранилища подтвердило единственный ключ `domianA4DraftV2`.  
Повторяемость: 1/1.  
Скриншот: `.playwright-cli/page-2026-07-27T09-02-19-496Z.png`  
Консоль: ошибок сценария нет; это не подтверждает корректность данных.  
Затронутые данные: сохранённые черновики обычного A4 и мотивационного режима.  
Предполагаемая причина: оба режима используют один storage-key и одну схему состояния.  
Файлы: `assets/js/app.js`, `index.html`, `motivation-calculator.html`.  
Исправление: не выполнено; требуется отдельное версионированное хранилище мотивационного режима и явная безопасная миграция/импорт без перезаписи A4.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-002

ID: MOT-QA-002  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: пакет «Новичок», шкала процентов  
Предусловия: чистая форма; один агент; пакет «Новичок»; точный режим.  
Шаги воспроизведения:
1. Добавить семь сделок по 100 000 ₽ реальными действиями браузера.
2. Проверить процент каждой строки и общую выплату.

Ожидаемый результат: согласно документу «Обновленная мотивация карьерного роста (4).docx» диапазон пакета «Новичок» составляет 30–40%; проценты не превышают 40%.  
Фактический результат: проценты `[30, 35, 40, 60, 65, 70, 80]`, выплата 380 000 ₽. Старые тесты A4, напротив, закрепляют переход к партнёрской шкале после третьей сделки, поэтому обнаружен конфликт источников.  
Повторяемость: браузер 1/1; независимый oracle 1/1.  
Скриншот: `.playwright-cli/page-2026-07-27T08-59-37-293Z.png`  
Консоль: ошибок нет.  
Затронутые данные: проценты сделок и выплата новичку после третьей сделки.  
Предполагаемая причина: пакет задаёт только минимальный процент, но не ограничивает верхнюю границу 40%.  
Файлы: `motivation-policy-2026.js`, `motivation-calculator-engine.js`, `assets/js/calculations.js`.  
Исправление: BLOCKED — требуется бизнес-решение по конфликту документа и утверждённой старой логики A4.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle падает; production-тесты ошибочно считают текущее поведение нормой.

## MOT-QA-003

ID: MOT-QA-003  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: пакет «Новичок», маленькая первая сделка  
Предусловия: пакет «Новичок»; точный режим.  
Шаги воспроизведения:
1. Ввести сделки 30 000 ₽, 100 000 ₽, 100 000 ₽.
2. Проверить процент каждой сделки независимой формулой.

Ожидаемый результат: по диапазону пакета первая ставка не выше 40%; независимый oracle ожидает `[30, 30, 35]`, пока не утверждено отдельное правило для маленькой сделки.  
Фактический результат: `[45, 30, 35]`; маленькая сделка получила 45%.  
Повторяемость: независимый oracle 1/1.  
Скриншот: не создан; воспроизведение зафиксировано выводом независимого теста.  
Консоль: не применимо.  
Затронутые данные: ставка и выплата по маленькой сделке новичка.  
Предполагаемая причина: к маленькой сделке применяется партнёрская ставка первой ступени вместо правил пакета.  
Файлы: `assets/js/calculations.js`, `motivation-calculator-engine.js`.  
Исправление: BLOCKED — документ не описывает отдельно влияние маленькой сделки на ступень; требуется бизнес-решение.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle падает.

## MOT-QA-004

ID: MOT-QA-004  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: таблица сравнения пакетов  
Предусловия: агент со статусом «Партнёр» и заполненными сделками.  
Шаги воспроизведения:
1. Выбрать агента для сравнения.
2. Рассчитать все семь строк таблицы.
3. Пассивно сравнить входной статус каждой строки с исходным статусом агента.

Ожидаемый результат: по утверждённому требованию пользователя во всех строках меняется только пакет; статус остаётся одним и тем же.  
Фактический результат: строка «Новичок» принудительно меняет статус с `partner` на `trainee`.  
Повторяемость: независимый oracle 1/1.  
Скриншот: не создан; поведение зафиксировано независимым тестом.  
Консоль: не применимо.  
Затронутые данные: ставка, выплата, вклад и точка окупаемости строки «Новичок».  
Предполагаемая причина: `applyPackage` изменяет статус вместе с пакетом.  
Файлы: `motivation-calculator-engine.js`, `assets/js/app.js`.  
Исправление: BLOCKED — требование одинакового статуса конфликтует с естественным статусом пакета «Новичок»; требуется бизнес-решение.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle падает.

## MOT-QA-005

ID: MOT-QA-005  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`, потенциально все страницы с `data-money-input`  
Компонент: денежные поля  
Предусловия: точный режим; пустое поле суммы сделки.  
Шаги воспроизведения:
1. Реальным вводом набрать `300,50`.
2. Повторить с `300.50`.
3. Увести фокус.

Ожидаемый результат: значение не теряет копейки без явного правила округления и предупреждения.  
Фактический результат: оба значения превращаются в `301`; дробная часть молча округляется.  
Повторяемость: 2/2.  
Скриншот: не создан; видимое значение зафиксировано браузерным снимком состояния.  
Консоль: ошибок нет.  
Затронутые данные: все денежные значения с копейками.  
Предполагаемая причина: общий форматтер использует целочисленное округление.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; правило округления отсутствует — требуется зафиксировать бизнес-правило либо сохранять копейки.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-006

ID: MOT-QA-006  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`, потенциально все страницы с `data-money-input`  
Компонент: денежные поля / валидация  
Предусловия: пустое поле суммы сделки.  
Шаги воспроизведения:
1. Реальным вводом набрать `abc`.
2. Увести фокус.

Ожидаемый результат: поле остаётся пустым или показывает понятную ошибку; невалидный текст не становится валидной суммой.  
Фактический результат: поле показывает `0` без сообщения об ошибке.  
Повторяемость: 1/1.  
Скриншот: не создан; результат зафиксирован браузерным снимком состояния.  
Консоль: ошибок нет.  
Затронутые данные: денежные поля; пользователь не отличит ошибку ввода от намеренного нуля.  
Предполагаемая причина: невалидный ввод нормализуется в ноль.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; нужна явная валидация без подмены значения.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-007

ID: MOT-QA-007  
Серьёзность: CRITICAL  
Страница: `motivation-calculator.html`, потенциально все страницы с `data-money-input`  
Компонент: денежные поля / отрицательные значения  
Предусловия: пустое поле суммы сделки.  
Шаги воспроизведения:
1. Нажать клавишу `-`.
2. Набрать `100`.
3. Увести фокус.

Ожидаемый результат: отрицательная сумма отклоняется с понятным сообщением либо остаётся `-100` для последующей проверки; знак нельзя молча отбрасывать.  
Фактический результат: поле становится положительным `100`, поэтому ошибочный отрицательный ввод превращается в реальную доходную сделку.  
Повторяемость: 1/1.  
Скриншот: не создан; результат зафиксирован браузерным снимком состояния.  
Консоль: ошибок нет.  
Затронутые данные: сделки, расходы, резервы и все прочие денежные поля с общим обработчиком.  
Предполагаемая причина: обработчик удаляет все символы, кроме цифр, до валидации.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; нужна валидация знака до нормализации.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-008

ID: MOT-QA-008  
Серьёзность: MINOR  
Страница: минимум `start.html`, `table-ledger.html`  
Компонент: статические ресурсы  
Предусловия: запущен локальный HTTP-сервер.  
Шаги воспроизведения:
1. Открыть страницу.
2. Проверить console/network.

Ожидаемый результат: страница не запрашивает отсутствующие ресурсы.  
Фактический результат: запрос `http://127.0.0.1:4173/favicon.ico` возвращает 404 и пишет ошибку в консоль.  
Повторяемость: 2/2 страницы.  
Скриншот: не требуется; ошибка сохранена в журнале Playwright.  
Консоль: `Failed to load resource: the server responded with a status of 404 (File not found)`.  
Затронутые данные: нет; журнал ошибок загрязнён и может скрывать новые ошибки.  
Предполагаемая причина: отсутствует favicon или явный корректный `<link rel="icon">`.  
Файлы: HTML-страницы, отсутствующий ресурс favicon.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-009

ID: MOT-QA-009  
Серьёзность: MAJOR  
Страница: `career.html`  
Компонент: создание и сохранение карьерного профиля  
Предусловия: чистый браузерный контекст; пустой `localStorage`.  
Шаги воспроизведения:
1. Открыть `career.html`.
2. Не нажимать «Новый профиль» и «Сохранить профиль».
3. Пассивно прочитать список ключей и значение карьерного хранилища.

Ожидаемый результат: одно открытие страницы без действия пользователя не создаёт сохранённый профиль.  
Фактический результат: в `domianCareerDraftV1` автоматически записан профиль `Новый агент`.  
Повторяемость: 1/1.  
Скриншот: не создан; содержимое подтверждено пассивным чтением хранилища.  
Консоль: ошибок нет.  
Затронутые данные: карьерное хранилище; пустое посещение создаёт ложную запись.  
Предполагаемая причина: начальная отрисовка немедленно вызывает сохранение состояния.  
Файлы: `career-mode.js`, `career-storage.js`.  
Исправление: не выполнено; сохранить только после явного пользовательского действия или осмысленного автосохранения с понятной индикацией.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-010

ID: MOT-QA-010  
Серьёзность: MAJOR  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: пользовательские описания правил  
Предусловия: открыть обычный или мотивационный калькулятор.  
Шаги воспроизведения:
1. Найти объяснения ставки и партнёрских условий.
2. Прочитать текст без знания внутренней терминологии кода.

Ожидаемый результат: по явному требованию пользователя все объяснения понятны собственнику без знания слова `floor` и внутренних расчётных терминов.  
Фактический результат: видны фразы `стартовый процент-floor`, `Калькулятор применит floor…`, `…не влияют на floor.`; также обнаружены требующие проверки термины `управленческая распределённая оценка`, `мотивационный резерв`, `квалифицируется`, `источник ставки`.  
Повторяемость: 3 подтверждённых вхождения `floor`; остальные термины требуют полной инвентаризации текстов.  
Скриншот: до исправления — `.playwright-cli/page-2026-07-27T08-59-37-293Z.png`; после исправления — `.playwright-cli/page-2026-07-27T10-23-27-305Z.png` и `.playwright-cli/page-2026-07-27T10-24-26-552Z.png`.  
Консоль: не применимо.  
Затронутые данные: нет; затронуто понимание результата и риск неверного управленческого решения.  
Предполагаемая причина: внутренние названия алгоритмов выведены пользователю без перевода на простой язык.  
Файлы: `index.html`, `start.html`, `career.html`, `assets/js/app.js`, `assets/js/career-mode.js`, `assets/js/domain/career-engine.js`, `assets/js/domain/benefit-engine.js`.  
Исправление: выполнено по прямому указанию пользователя. В интерфейсе введён единый простой словарь; подробная карта замен записана в `02-readable-language.md`. Внутренние имена полей и функций не переименовывались, чтобы не смешивать текстовую правку с рефакторингом.  
Повторная проверка: PASS — через реальные браузерные действия выбран пакет «Премиум», введена сделка 100 000 ₽; отдельно подтверждён и выбран 7-й уровень на странице профилей. В видимом интерфейсе нет `floor`, `max`, «плательщик», «стажевой/результативный трек» и прежних двусмысленных подписей.  
Регрессионные тесты: `node --test tests/motivation-calculator.test.js tests/career-engine.test.js` — 25/25 PASS; полный регрессионный прогон ещё не выполнен.

## MOT-QA-011

ID: MOT-QA-011  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: свёрнутая карточка агента  
Предусловия: создать минимум три агента и выбрать им разные пакеты.  
Шаги воспроизведения:
1. Агенту Alpha оставить «Стандарт», Beta выбрать «Премиум», Gamma — «Индивидуальный».
2. Ввести соответственно 100 000 ₽, 200 000 ₽ и 300 000 ₽.
3. Свернуть карточки Beta и Gamma.
4. Сравнить название пакета в заголовке с выбранным пакетом и выплатой.

Ожидаемый результат: свёрнутая карточка показывает фактически выбранный пакет каждого агента.  
Фактический результат: у Beta и Gamma заголовок показывает `Партнёр · Стандарт`, хотя выплаты рассчитаны по 60% и 70% и внутри выбраны «Премиум» и «Индивидуальный».  
Повторяемость: 2/2 агентов.  
Скриншот: текущий снимок Playwright-сессии `linkage`; постоянный PNG будет сохранён до исправления.  
Консоль: ошибок нет.  
Затронутые данные: расчёт не искажён, но интерфейс сообщает пользователю неверный пакет.  
Предполагаемая причина: заголовок использует старое поле схемы оплаты вместо `careerPackageId`.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-012

ID: MOT-QA-012  
Серьёзность: MAJOR  
Страница: `career.html`  
Компонент: `career-storage`, контроль версии  
Предусловия: в карьерном storage-key находится корректный JSON с неизвестной версией `999`.  
Шаги воспроизведения:
1. Подготовить изолированный storage-fixture версии `999` с существующим профилем.
2. Вызвать публичное чтение карьерного хранилища.
3. Проверить результат и предупреждения.

Ожидаемый результат: по явному требованию пользователя неизвестная версия не загружается молча; исходные данные сохраняются, пользователь получает понятное сообщение.  
Фактический результат: версия `999` без предупреждения нормализована в версию `1`, профиль принят как обычный.  
Повторяемость: 1/1.  
Скриншот: не применимо; дефект подтверждён изолированным storage-тестом.  
Консоль: предупреждений нет.  
Затронутые данные: карьерные профили, решения и сопоставления будущих/неподдерживаемых версий.  
Предполагаемая причина: `normalizeStore` игнорирует входное поле `version`, а `read` не проверяет его до нормализации.  
Файлы: `assets/js/career-storage.js`, `assets/js/career-mode.js`.  
Исправление: не выполнено; нужен отказ от загрузки с видимым сообщением и сохранением исходного значения.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: `storage-resilience.test.js` — FAILED.

## MOT-QA-013

ID: MOT-QA-013  
Серьёзность: CRITICAL  
Страница: `career.html`  
Компонент: восстановление после повреждённого JSON  
Предусловия: карьерный storage-key содержит повреждённый JSON `{"version":1,"profiles":[`.  
Шаги воспроизведения:
1. Подготовить изолированный storage-fixture с повреждённым значением.
2. Выполнить чтение тем же публичным API, которым пользуется страница.
3. Повторить автоматическое создание профиля, выполняемое страницей при пустом результате.
4. Сравнить исходное значение storage-key с итоговым.

Ожидаемый результат: повреждённое пользовательское значение не перезаписывается; создаётся резервная копия и выводится понятное сообщение о восстановлении.  
Фактический результат: чтение возвращает пустое состояние, после чего создание `Новый агент` полностью заменяет повреждённое исходное значение; резервной копии нет.  
Повторяемость: 1/1.  
Скриншот: не применимо; дефект подтверждён изолированным storage-тестом.  
Консоль: есть техническое `console.warn`, но пользовательского сообщения нет.  
Затронутые данные: все профили, решения и сопоставления в повреждённом карьерном хранилище.  
Предполагаемая причина: ошибка чтения не передаётся вызывающему коду; `initialize` считает состояние пустым и вызывает записывающий `createProfile`.  
Файлы: `assets/js/career-storage.js`, `assets/js/career-mode.js`.  
Исправление: не выполнено; нужен режим read-error, резервная копия сырого значения и запрет автоматической записи.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: `storage-resilience.test.js` — FAILED.

## MOT-QA-014

ID: MOT-QA-014  
Серьёзность: MAJOR  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: кнопка «Добавить агента»  
Предусловия: форма с одним агентом.  
Шаги воспроизведения:
1. Один раз нажать «Добавить агента» и проверить, что добавлен один агент.
2. Быстро дважды нажать ту же кнопку.
3. Посчитать карточки агентов.

Ожидаемый результат: одно пользовательское двойное нажатие создаёт не более одного нового агента.  
Фактический результат: после обычного нажатия агентов стало 2, после двойного — 4; двойное нажатие создало сразу двух агентов.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM после реальных кликов.  
Консоль: ошибок нет.  
Затронутые данные: список агентов, распределение расходов, итог офиса и таблица сравнения.  
Предполагаемая причина: у обработчика нет защиты от повторного события.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-015

ID: MOT-QA-015  
Серьёзность: CRITICAL  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: кнопка «Удалить агента»  
Предусловия: четыре агента; у первого и последнего заполнены данные; два средних пустые.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить» у второго агента.
2. Посчитать оставшиеся карточки.
3. Проверить соседние имена.

Ожидаемый результат: удаляется только выбранный второй агент.  
Фактический результат: количество карточек уменьшилось с 4 до 2; удалены сразу оба средних агента. Первый и последний сохранились.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM после реального двойного клика.  
Консоль: ошибок нет.  
Затронутые данные: полностью теряются профиль ввода и все сделки дополнительного агента.  
Предполагаемая причина: повторный click обрабатывается после перерисовки на элементе, занявшем ту же позицию.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; нужна одноразовая блокировка destructive-action и привязка подтверждения к стабильному ID.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-016

ID: MOT-QA-016  
Серьёзность: CRITICAL  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: кнопка удаления расхода  
Предусловия: пять строк расходов; в первой заполнены название и сумма.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить» во второй строке.
2. Посчитать строки расходов.
3. Проверить данные первой строки.

Ожидаемый результат: удаляется только выбранная строка; строк становится 4.  
Фактический результат: строк стало 3; одним двойным нажатием удалены две соседние статьи. Первая строка сохранилась.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM после реального двойного клика.  
Консоль: ошибок нет.  
Затронутые данные: статьи постоянных расходов и итог собственнику.  
Предполагаемая причина: повторное событие применяется к новой строке на той же позиции после ререндера.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-017

ID: MOT-QA-017  
Серьёзность: CRITICAL  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: кнопка удаления сделки  
Предусловия: у агента три сделки; первая заполнена суммой 77 777 ₽.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить» у второй сделки.
2. Посчитать строки сделок.
3. Проверить сумму первой сделки.

Ожидаемый результат: удаляется только вторая сделка; остаются две строки.  
Фактический результат: строк стало с 3 сразу 1; удалены вторая и третья сделки. Первая сумма 77 777 ₽ сохранилась.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM после реального двойного клика.  
Консоль: ошибок нет.  
Затронутые данные: сделки агента, его выплата, реферал, роялти и итог офиса.  
Предполагаемая причина: повторное событие применяется к новой строке на той же позиции после ререндера.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-018

ID: MOT-QA-018  
Серьёзность: CRITICAL  
Страница: `table-ledger.html`  
Компонент: «Загрузить из A4» / личные сделки собственника  
Предусловия: в A4 восстановлен пример с личными сделками собственника 150 000 ₽; создан snapshot кнопкой «Табличный режим».  
Шаги воспроизведения:
1. Открыть созданную ведомость.
2. Нажать «Загрузить из A4».
3. Проверить поле «Личные сделки собственника» и итоговую сводку.

Ожидаемый результат: поле и сводка показывают одну и ту же импортированную сумму 150 000 ₽.  
Фактический результат: поле показывает `0`, а «Общий оборот» и «Итог с собственником» уже рассчитаны с 150 000 ₽. Пользователь видит противоречащие друг другу данные; изменение поля с видимым нулём может затереть импортированную сумму.  
Повторяемость: 1/1.  
Скриншот: будет сохранён до исправления; состояние подтверждено браузерным снимком.  
Консоль: только отдельная ошибка favicon 404.  
Затронутые данные: личные сделки собственника и итог офиса в ведомости.  
Предполагаемая причина: импорт обновляет `state.ownerSales`, но `render()` не синхронизирует статическое поле `[data-office-field="ownerSales"]`.  
Файлы: `assets/js/table-ledger.js`, `table-ledger.html`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-019

ID: MOT-QA-019  
Серьёзность: CRITICAL  
Страница: `motivation-calculator.html` → `table-ledger.html`  
Компонент: переход в ведомость / передача данных  
Предусловия: чистый браузерный контекст; в мотивационном режиме агент `Премиум QA`, пакет «Премиум», сделка 200 000 ₽, реферал включён.  
Шаги воспроизведения:
1. Через интерфейс заполнить агента в мотивационном режиме.
2. Нажать верхнюю ссылку «Ведомость сделок».
3. В открытой ведомости нажать «Загрузить из A4».
4. Проверить имя, сделку, пакет, выплату и сообщение.

Ожидаемый результат: переход из мотивационного калькулятора передаёт именно текущий расчёт выбранного режима в ведомость.  
Фактический результат: ведомость открылась пустой; после «Загрузить из A4» сообщила `Snapshot A4 не найден`; имя `Премиум QA` и сделка отсутствуют. В мотивационном режиме нет кнопки, создающей snapshot, — только обычная ссылка.  
Повторяемость: 1/1.  
Скриншот: будет сохранён до исправления; сценарий зафиксирован выводом Playwright.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: все агенты, сделки, выбранные пакеты, рефералы, мотивации и итоги при переходе в ведомость.  
Предполагаемая причина: ссылка `table.html` не вызывает `openTableModePage`, а ведомость умеет импортировать только `domianA4TableSnapshot`.  
Файлы: `index.html`, `assets/js/app.js`, `assets/js/table-ledger.js`.  
Исправление: не выполнено; переход должен сохранить снимок текущего режима и явно загрузить/предложить его без потери пакета.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-020

ID: MOT-QA-020  
Серьёзность: MAJOR  
Страница: `table-ledger.html`  
Компонент: кнопки «+ Агент»  
Предусловия: чистая ведомость с одним агентом.  
Шаги воспроизведения:
1. Один раз нажать верхнюю кнопку «+ Агент».
2. Быстро дважды нажать её ещё раз.
3. Посчитать строки настройки агентов.

Ожидаемый результат: двойное нажатие создаёт не более одного нового агента.  
Фактический результат: после первого нажатия агентов стало 2, после двойного — 4; созданы сразу два агента.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: агенты ведомости и её итоги.  
Предполагаемая причина: нет защиты от повторного события.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-021

ID: MOT-QA-021  
Серьёзность: MAJOR  
Страница: `table-ledger.html`  
Компонент: кнопки «+ Расход»  
Предусловия: чистая ведомость с шестью строками расходов.  
Шаги воспроизведения:
1. Один раз нажать «+ Расход».
2. Быстро дважды нажать её ещё раз.
3. Посчитать строки расходов.

Ожидаемый результат: двойное нажатие создаёт не более одной новой строки.  
Фактический результат: после первого нажатия строк стало 7, после двойного — 9; двойное нажатие создало две строки.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: статьи расходов ведомости.  
Предполагаемая причина: нет защиты от повторного события.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-022

ID: MOT-QA-022  
Серьёзность: MAJOR  
Страница: `table-ledger.html`  
Компонент: кнопки «+ Сделка»  
Предусловия: четыре агента, по одной сделке у каждого.  
Шаги воспроизведения:
1. Один раз нажать верхнюю кнопку «+ Сделка».
2. Быстро дважды нажать её ещё раз.
3. Посчитать строки сделок.

Ожидаемый результат: двойное нажатие добавляет последнему агенту не более одной сделки.  
Фактический результат: строк было 4, после первого нажатия стало 5, после двойного — 7; добавлены две строки.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: сделки последнего агента и итог ведомости.  
Предполагаемая причина: нет защиты от повторного события.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-023

ID: MOT-QA-023  
Серьёзность: CRITICAL  
Страница: `table-ledger.html`  
Компонент: удаление расхода  
Предусловия: девять строк расходов.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить» у второй строки.
2. Подтвердить удаление.
3. Посчитать строки.

Ожидаемый результат: удаляется только выбранная строка; остаётся 8.  
Фактический результат: осталось 7 строк; удалены две соседние статьи.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: две статьи расходов и итог офиса.  
Предполагаемая причина: оба click-события проходят обработчик после перерисовки.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-024

ID: MOT-QA-024  
Серьёзность: CRITICAL  
Страница: `table-ledger.html`  
Компонент: удаление сделки  
Предусловия: у последнего агента четыре пустые строки сделок.  
Шаги воспроизведения:
1. Быстро дважды нажать `×` во второй строке.
2. Посчитать сделки этого агента.

Ожидаемый результат: удаляется только выбранная строка; остаются 3.  
Фактический результат: осталось 2 строки; одним двойным нажатием удалены две сделки.  
Повторяемость: 1/1.  
Скриншот: не создан; счётчики получены из видимого DOM.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: сделки, выплаты и итог ведомости.  
Предполагаемая причина: оба click-события проходят обработчик после перерисовки.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-025

ID: MOT-QA-025  
Серьёзность: MAJOR  
Страница: `career.html`  
Компонент: кнопка «Новый»  
Предусловия: один сохранённый карьерный профиль.  
Шаги воспроизведения:
1. Один раз нажать «Новый».
2. Быстро дважды нажать «Новый».
3. Посчитать профили в списке.

Ожидаемый результат: двойное нажатие создаёт не более одного нового профиля.  
Фактический результат: профилей было 1, после обычного нажатия стало 2, после двойного — 4; созданы два одинаковых `Новый агент`.  
Повторяемость: 1/1.  
Скриншот: не создан; список и счётчики получены из видимого DOM.  
Консоль: ошибок нет.  
Затронутые данные: карьерные профили; возникают ложные дубликаты с разными ID.  
Предполагаемая причина: нет защиты от повторного события.  
Файлы: `assets/js/career-mode.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-026

ID: MOT-QA-026  
Серьёзность: CRITICAL  
Страница: `career.html`  
Компонент: кнопка «Удалить» профиль  
Предусловия: четыре профиля; текущий — пустой `Новый агент`; первым в списке расположен заполненный `Карьерный QA` с сохранённым решением.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить».
2. Подтвердить первое удаление.
3. Наблюдать второе подтверждение и подтвердить его.
4. Проверить список профилей.

Ожидаемый результат: удаляется только текущий профиль `Новый агент`.  
Фактический результат: второе событие после перерисовки выбрало уже профиль `Карьерный QA`; количество профилей уменьшилось с 4 до 2, заполненный профиль и его решение потеряны.  
Повторяемость: 1/1.  
Скриншот: не создан; два разных текста confirm и итоговый список зафиксированы Playwright.  
Консоль: ошибок нет.  
Затронутые данные: профиль, история решений и сопоставления с A4 второго сотрудника.  
Предполагаемая причина: destructive-action не блокируется после первого клика; текущий профиль меняется до обработки второго события.  
Файлы: `assets/js/career-mode.js`, `assets/js/career-storage.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-027

ID: MOT-QA-027  
Серьёзность: MAJOR  
Страница: `career.html`  
Компонент: индивидуальная реклама / отображение копеек  
Предусловия: итоговый пакет «Индивидуальный»; задатки предыдущего месяца 499 999 ₽.  
Шаги воспроизведения:
1. Через интерфейс выбрать/получить пакет «Индивидуальный».
2. Ввести 499 999 ₽ в поле предыдущего месяца.
3. Проверить строку «Индивидуальная реклама».

Ожидаемый результат: по формуле 3% значение составляет 14 999,97 ₽ либо применяется заранее утверждённое и явно описанное правило округления.  
Фактический результат: интерфейс показывает 15 000 ₽ без пояснения об округлении. Правило округления в источниках истины не найдено.  
Повторяемость: 1/1.  
Скриншот: будет сохранён при повторном сценарии перед исправлением.  
Консоль: ошибок нет.  
Затронутые данные: офисный расход и отображаемый итог мотиваций.  
Предполагаемая причина: общий `money`-форматтер округляет до целого рубля.  
Файлы: `assets/js/career-mode.js`, `assets/js/domain/benefit-engine.js`.  
Исправление: BLOCKED — требуется бизнес-решение о копейках и направлении округления.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle подтверждает 14 999,97 ₽ до UI-форматирования.

## MOT-QA-028

ID: MOT-QA-028  
Серьёзность: MAJOR  
Страница: `table-ledger.html`  
Компонент: денежные поля / копейки  
Предусловия: пустая строка сделки.  
Шаги воспроизведения:
1. Клавиатурой ввести `300,50` и уйти из поля клавишей Tab.
2. Повторить с `300.50`.

Ожидаемый результат: копейки не теряются без утверждённого правила округления и явного пояснения.  
Фактический результат: оба значения отображаются как `301`; округление произошло молча.  
Повторяемость: 2/2.  
Скриншот: не создан; матрица значений зафиксирована выводом Playwright.  
Консоль: отдельная ошибка favicon 404.  
Затронутые данные: сделки, расходы, выплаты и итог ведомости с копейками.  
Предполагаемая причина: `formatInputMoney` вызывает `Math.round`.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: BLOCKED — требуется единое бизнес-правило округления.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-029

ID: MOT-QA-029  
Серьёзность: CRITICAL  
Страница: `career.html`  
Компонент: «Индивидуальный договорный минимум, %» / валидация  
Предусловия: новый карьерный профиль.  
Шаги воспроизведения:
1. Клавиатурой ввести `300` в поле договорного минимума, у которого указан `max=100`.
2. Убедиться пассивным чтением validity, что браузер считает значение невалидным.
3. Нажать «Сохранить профиль» и «Сохранить карьерное решение».
4. Перезагрузить страницу.

Ожидаемый результат: сохранение блокируется; пользователь видит понятное сообщение, что допустим диапазон 0–100%.  
Фактический результат: кнопки обходят HTML-валидацию; предварительный расчёт молча показывает минимум 100%, а после перезагрузки введённые 300% заменены на 100% без сообщения.  
Повторяемость: 1/1.  
Скриншот: будет сохранён при повторной проверке.  
Консоль: ошибок нет.  
Затронутые данные: минимальный процент, выплата агенту и экономика офиса.  
Предполагаемая причина: JS-кнопки не вызывают `checkValidity/reportValidity`, а engine/storage молча ограничивают число диапазоном.  
Файлы: `career.html`, `assets/js/career-mode.js`, `assets/js/career-storage.js`, `assets/js/domain/career-engine.js`.  
Исправление: не выполнено; нужна явная валидация до расчёта и сохранения.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-030

ID: MOT-QA-030  
Серьёзность: MAJOR  
Страница: `career.html`  
Компонент: денежные поля / отрицательные значения  
Предусловия: новый карьерный профиль.  
Шаги воспроизведения:
1. Клавиатурой ввести `-100` в «Задатки предыдущего месяца».
2. Убедиться пассивным чтением validity, что значение невалидно.
3. Нажать «Сохранить карьерное решение».
4. Перезагрузить страницу.

Ожидаемый результат: сохранение блокируется с понятным сообщением об отрицательной сумме.  
Фактический результат: решение сохраняется; после перезагрузки поле пустое, то есть `-100` молча превращено в 0. Такое же преобразование выполняет общий `numberValue` для остальных денежных полей страницы.  
Повторяемость: 1/1; матрицей проверены семь numeric-полей.  
Скриншот: будет сохранён при повторной проверке.  
Консоль: ошибок нет.  
Затронутые данные: задатки, квартальная комиссия и расходы мотиваций.  
Предполагаемая причина: JS-кнопка игнорирует HTML-валидацию, `numberValue` применяет `Math.max(0, value)`.  
Файлы: `career.html`, `assets/js/career-mode.js`.  
Исправление: не выполнено; нужна явная ошибка без изменения введённого значения.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-031

ID: MOT-QA-031  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: ручной процент сделки ниже минимума пакета  
Предусловия: пакет «Премиум» с гарантированным минимумом 60%; одна сделка 100 000 ₽; стандартная система.  
Шаги воспроизведения:
1. Ввести ручной процент 70%, затем 60% и проверить расчёт.
2. Ввести 50%.
3. Повторить с 0%.
4. Прочитать подсказку и применённый процент строки.

Ожидаемый результат: поведение ручного процента ниже гарантированного минимума должно быть утверждено бизнесом; до этого интерфейс не должен молча принимать ввод как действующий.  
Фактический результат: поле сохраняет видимые `50` или `0`, подсказка продолжает говорить `Меняет только эту сделку`, но расчёт молча применяет 60%. Значения 70%, 60%, пустое и 100% дали соответственно 70%, 60%, 60% и 100%.  
Повторяемость: 2/2 значений ниже минимума.  
Скриншот: после исправления — `.playwright-cli/page-2026-07-27T10-23-27-305Z.png`; таблица с новой подписью есть в `.playwright-cli/page-2026-07-27T10-23-13-110Z.yml`.  
Консоль: ошибок нет.  
Затронутые данные: ставка строки и ожидания пользователя от ручного исключения.  
Предполагаемая причина: engine применяет `max(manualRate, packageFloor)`, но UI не валидирует и не объясняет отклонение введённого значения.  
Файлы: `assets/js/calculations.js`, `assets/js/domain/motivation-calculator-engine.js`, `assets/js/app.js`.  
Исправление: BLOCKED — требуется бизнес-решение: запрещать значение, повышать его до минимума или разрешать явное исключение. В любом варианте нужен понятный результат в строке.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle считает ниже-minimum сценарий BLOCKED, а не PASS.

## MOT-QA-032

ID: MOT-QA-032  
Серьёзность: CRITICAL  
Страница: `index.html`, `motivation-calculator.html`  
Компонент: роялти на границах диапазонов / ввод копеек  
Предусловия: один агент в быстром режиме; расходы, мотивации и сделки собственника равны нулю.  
Шаги воспроизведения:
1. Ввести комиссию `499 999,99` ₽.
2. Повторить для сумм на 1 копейку ниже 750 000, 1 000 000, 1 500 000, 2 000 000, 2 500 000, 3 000 000 и 4 000 000 ₽.
3. Сравнить видимую сумму и роялти с независимой таблицей ставок.

Ожидаемый результат: сумма остаётся ниже границы и рассчитывается по предыдущей ставке; например, 499 999,99 ₽ × 7% ≈ 35 000 ₽.  
Фактический результат: каждое значение округляется вверх до границы до расчёта. Для 499 999,99 ₽ поле стало 500 000 ₽, роялти — 32 500 ₽; аналогично 749 999,99 → 750 000 и 45 000 ₽ вместо ≈48 750 ₽. Все семь промежуточных границ перескочили в следующий диапазон. Точные значения самих границ рассчитаны корректно.  
Повторяемость: 8/8 значений «граница минус 1 копейка».  
Скриншот: будет сохранён при повторной проверке.  
Консоль: ошибок нет.  
Затронутые данные: роялти и итог собственнику возле каждой границы оборота.  
Предполагаемая причина: общий money-input округляет значение до целого рубля до вызова расчёта роялти.  
Файлы: `assets/js/app.js`, `assets/js/calculations.js`.  
Исправление: BLOCKED в части общего правила округления; как минимум нельзя менять диапазон без явного утверждённого правила.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: независимый oracle проходит на точных числах; браузерная матрица границ — FAILED.

## MOT-QA-033

ID: MOT-QA-033  
Серьёзность: CRITICAL  
Страница: `motivation-calculator.html`  
Компонент: карточка агента / вклад и точка окупаемости  
Предусловия: чистая форма; один агент; пакет «Стандарт».  
Шаги воспроизведения:
1. Клавиатурой ввести сделку 100 000 ₽ и расход офиса 100 000 ₽.
2. Не переключать пакет и не выполнять другое структурное действие.
3. Проверить карточку агента.
4. Повторно выбрать уже выбранный пакет «Стандарт».

Ожидаемый результат: вклад, доля расходов, оценка роялти и точка окупаемости появляются и пересчитываются сразу после ввода суммы.  
Фактический результат: до повторного выбора пакета строки `Роялти-оценка`, `Доля расходов`, `Вклад агента` и `Окупится от` отсутствуют. После лишнего структурного действия они появляются; точка — 209 000 ₽, вклад — −52 000 ₽. При дальнейших изменениях денежных полей эти строки также остаются старыми до нового структурного ререндера.  
Повторяемость: 1/1, затем повторено при изменении суммы с двумя агентами.  
Скриншот: будет сохранён до исправления.  
Консоль: ошибок нет.  
Затронутые данные: пользователь может принять решение по отсутствующей или устаревшей окупаемости.  
Предполагаемая причина: первоначальный `renderAgents` не имеет `agentEconomics` для пустого агента; `updateTotalsOnly` обновляет общий итог, но не добавляет/обновляет расширенные строки карточки.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; экономические метрики должны иметь адресные DOM-узлы и обновляться на каждом изменении либо выполняться безопасный ререндер с сохранением ввода.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-034

ID: MOT-QA-034  
Серьёзность: CRITICAL  
Страница: `motivation-calculator.html`  
Компонент: две строки «Выплата агенту» внутри одной карточки  
Предусловия: чистая форма; одна сделка 100 000 ₽; пакет «Стандарт».  
Шаги воспроизведения:
1. Ввести сумму сделки клавиатурой.
2. Сравнить «Итого зарплата агенту» в блоке сделок и нижнюю строку «Выплата агенту».
3. Повторно выбрать пакет «Стандарт».

Ожидаемый результат: обе строки одновременно показывают 45 000 ₽.  
Фактический результат: верхняя строка показывает 45 000 ₽, нижняя — 0 ₽. После лишнего переключения пакета нижняя строка становится 45 000 ₽.  
Повторяемость: 1/1.  
Скриншот: будет сохранён до исправления.  
Консоль: ошибок нет.  
Затронутые данные: пользователь видит два разных ответа о выплате одному агенту.  
Предполагаемая причина: в карточке два узла с одинаковым `data-agent-summary="payout"`, а адресное обновление меняет только первый `querySelector`.  
Файлы: `assets/js/app.js`.  
Исправление: не выполнено; нужны уникальные назначения либо обновление всех соответствующих узлов.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — фаза A.

## MOT-QA-035

ID: MOT-QA-035  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: подпись точки окупаемости  
Предусловия: любой агент с ненулевой долей расходов.  
Шаги воспроизведения:
1. Получить строку `Окупится от 209 000 ₽`.
2. Проверить алгоритм поиска в независимом тесте и исходном модуле.
3. Сравнить значения 208 000 и 209 000 ₽ отдельной формулой.

Ожидаемый результат: при поиске шагом 1 000 ₽ интерфейс явно пишет `примерно от`; точное значение не заявляется.  
Фактический результат: интерфейс пишет без оговорки `Окупится от`, хотя алгоритм сначала идёт шагом 10 000 ₽, затем уточняет только шагом 1 000 ₽. В проверенном простом случае 208 000 ₽ дали отрицательный вклад, 209 000 ₽ — положительный, то есть сетка сработала, но точность всё равно ограничена тысячей.  
Повторяемость: все строки карточки и таблицы сравнения.  
Скриншот: будет сохранён до исправления.  
Консоль: ошибок нет.  
Затронутые данные: восприятие точности управленческого порога.  
Предполагаемая причина: подпись не отражает дискретность `findBreakEvenCommission`.  
Файлы: `assets/js/app.js`, `assets/js/domain/motivation-calculator-engine.js`.  
Исправление: выполнено только в тексте: карточка пишет `Примерно окупится при комиссии от`, таблица — `Окупится примерно при комиссии от`. Алгоритм не менялся.  
Повторная проверка: PASS для подписи — проверено в карточке и таблице после реального выбора пакета и ввода сделки.  
Регрессионные тесты: точность сетки проверена на простом сценарии; полный набор ещё выполняется.

## MOT-QA-036

ID: MOT-QA-036  
Серьёзность: MAJOR  
Страница: `motivation-calculator.html`  
Компонент: короткий вывод для собственника / карточка агента  
Предусловия: один агент, пакет «Премиум», сделка 100 000 ₽, расходы офиса и мотивации равны нулю.  
Шаги воспроизведения:
1. Выбрать пакет «Премиум».
2. Клавиатурой ввести сделку 100 000 ₽.
3. Прокрутить до итогового вывода по агенту.

Ожидаемый результат: статус, объяснение и рекомендация согласованы; при остатке офису +33 000 ₽ текст не сообщает о скрытом отрицательном результате.  
Фактический результат: статус говорит `Офис получает прибыль`, сумма равна `33 000 ₽`, но строка `Причина` утверждает `вклад ниже безопасного уровня при текущей комиссии и расходах`.  
Повторяемость: 1/1.  
Скриншот: до исправления — `.playwright-cli/page-2026-07-27T10-21-46-891Z.yml`; после исправления — `.playwright-cli/page-2026-07-27T10-23-27-305Z.png`.  
Консоль: ошибок нет.  
Затронутые данные: управленческая интерпретация корректно рассчитанного положительного остатка.  
Предполагаемая причина: `getRiskReasons()` всегда возвращает отрицательную фразу по умолчанию, даже когда вклад положительный.  
Файлы: `assets/js/app.js`.  
Исправление: выполнено после фиксации дефекта. Для положительного остатка вывод сообщает, что комиссия покрывает выплаты и расходы; для отрицательного — что не покрывает. `Причина` заменена на `Что влияет на результат`, `Вклад` — на `Остаток офису`.  
Повторная проверка: PASS — тот же сценарий показывает `Офис получает прибыль`, `Остаток офису 33 000 ₽` и согласованное объяснение `комиссия покрывает выплату агенту, роялти, мотивации офиса и часть общих расходов`.  
Регрессионные тесты: `node --test tests/motivation-calculator.test.js tests/career-engine.test.js` — 25/25 PASS; полный регрессионный прогон ещё не выполнен.

## MOT-QA-037

ID: MOT-QA-037  
Серьёзность: MAJOR  
Страница: `simple.html`, `start.html`, навигация всех режимов  
Компонент: «Простой расчёт»  
Предусловия: перейти в простой режим по видимой ссылке из стартовой страницы или навигации.  
Шаги воспроизведения:
1. В поле «Общая комиссия агентов за месяц» ввести `2 400 000`.
2. В поле «Количество агентов» ввести `6`.
3. Заполнить любое поле расходов и изменить схему выплат.
4. Проверить блок «Итог».

Ожидаемый результат: режим, доступный из основной навигации как калькулятор, пересчитывает видимые итоги либо не предлагает интерактивные поля до подключения расчёта.  
Фактический результат: все поля принимают ввод, но ни одна итоговая строка не меняется; «Остаток собственнику» продолжает показывать `расчёт будет добавлен позже`. На странице есть предупреждение о каркасе, но визуально форма выглядит доступной.  
Повторяемость: 1/1; проверены текстовое и числовое поле.  
Скриншот: `.playwright-cli/page-2026-07-27T10-31-28-489Z.png`.  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: введённые значения не сохраняются и не участвуют ни в одном расчёте.  
Предполагаемая причина: `simple.html` — статический прототип без подключённого JS.  
Файлы: `simple.html`, `start.html`, общая навигация активных страниц.  
Исправление: не выполнено; либо скрыть/отключить режим и поля, либо реализовать расчёт с независимыми тестами.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: существующий тест проверяет только наличие статического каркаса, что не является доказательством работы.

## MOT-QA-038

ID: MOT-QA-038  
Серьёзность: CRITICAL  
Страница: `table-ledger.html`  
Компонент: кнопка «Удалить агента» / быстрое двойное нажатие  
Предусловия: в ведомости три агента.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить агента» у второго агента.
2. Подтвердить оба последовательно появившихся окна удаления.
3. Сосчитать оставшихся агентов.

Ожидаемый результат: удалён только второй агент; один пользовательский жест не должен переноситься на следующую строку.  
Фактический результат: последовательно появляются два подтверждения, после которых удаляются второй и третий агенты; из трёх остаётся один.  
Повторяемость: 1/1.  
Скриншот: состояние до — `.playwright-cli/page-2026-07-27T10-32-07-399Z.yml` (три кнопки удаления); после — снимок сессии `ledgerwording` после 13:32 (одна карточка агента).  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: полностью удаляются два агента, их сделки и мотивационные настройки.  
Предполагаемая причина: обработчик не защищён от второго `click` события внутри `dblclick`; после первого ререндера второй клик применяется к новой строке на том же месте.  
Файлы: `assets/js/table-ledger.js`.  
Исправление: не выполнено; требуется защита destructive-action от повторного события и полная повторная проверка одиночного/двойного нажатия.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: существующие unit-тесты проверяют только наличие подтверждения, но не реальное двойное нажатие.

## MOT-QA-039

ID: MOT-QA-039  
Серьёзность: MAJOR  
Страница: `extended.html`  
Компонент: кнопка «+ Агент» / быстрое двойное нажатие  
Предусловия: подробный режим, один начальный агент.  
Шаги воспроизведения:
1. Переключиться на «Подробный ввод по агентам и сделкам».
2. Быстро дважды нажать «+ Агент».
3. Сосчитать карточки агентов.

Ожидаемый результат: один пользовательский жест добавляет одного агента.  
Фактический результат: добавляются два агента; вместо двух карточек становится три.  
Повторяемость: 1/1.  
Скриншот: `.playwright-cli/page-2026-07-27T10-34-41-089Z.yml` содержит три кнопки удаления агентов после двойного нажатия.  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: появляются лишние пустые агенты и меняется распределение расходов при дальнейшем заполнении.  
Предполагаемая причина: каждый `click` внутри `dblclick` без защиты добавляет отдельный объект.  
Файлы: `assets/js/extended-mode.js`.  
Исправление: не выполнено.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — в существующих тестах нет браузерного double-click.

## MOT-QA-040

ID: MOT-QA-040  
Серьёзность: CRITICAL  
Страница: `extended.html`  
Компонент: кнопка «Удалить агента» / быстрое двойное нажатие  
Предусловия: подробный режим, три агента.  
Шаги воспроизведения:
1. Быстро дважды нажать «Удалить агента» у второго агента.
2. Сосчитать оставшиеся карточки.

Ожидаемый результат: удалён только второй агент.  
Фактический результат: удаляются второй и третий агенты; из трёх остаётся один. Подтверждение удаления отсутствует.  
Повторяемость: 1/1.  
Скриншот: до — `.playwright-cli/page-2026-07-27T10-35-11-833Z.yml`; после — `.playwright-cli/page-2026-07-27T10-35-46-029Z.yml`.  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: два агента со всеми сделками удаляются без возможности восстановления.  
Предполагаемая причина: после первого удаления и ререндера второй `click` применяется к следующей карточке на том же месте; защитного подтверждения и блокировки повторного события нет.  
Файлы: `assets/js/extended-mode.js`.  
Исправление: не выполнено; нужны защита от повторного события и подтверждение для заполненного агента.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: НЕ ВЫПОЛНЕНЫ — unit-тестов для удаления в Extended нет.

## MOT-QA-041

ID: MOT-QA-041  
Серьёзность: CRITICAL  
Страница: `extended.html`  
Компонент: переключение общего и подробного режима ввода  
Предусловия: выбран «Общий ручной ввод периода».  
Шаги воспроизведения:
1. В общий ввод внести комиссию 400 000 ₽, одного агента, четыре сделки и расход 100 000 ₽.
2. Убедиться, что активной подсвечена кнопка общего ввода.
3. Прокрутить ниже общего ввода.
4. Пассивно проверить свойства и вычисленные стили `quickModePanel` и `detailedModePanel`.

Ожидаемый результат: подробная форма скрыта и недоступна, пока выбран общий ввод.  
Фактический результат: видны одновременно общий ввод и подробная форма «Агенты и сделки». У подробной формы `hidden === true`, но вычисленный стиль остаётся `display: grid`; введённые в неё значения не участвуют в текущем итоге общего режима.  
Повторяемость: 1/1 после переключения подробный → общий.  
Скриншот: `.playwright-cli/page-2026-07-27T10-38-37-693Z.png`; пассивное чтение: `quickHidden=false`, `detailedHidden=true`, оба `display=grid`.  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: пользователь может заполнить визуально доступную, но неактивную форму и получить итог по другим значениям.  
Предполагаемая причина: авторское CSS-правило с `display:grid` перекрывает скрытие элемента; отсутствует обязательное `[hidden] { display: none !important; }`.  
Файлы: `assets/css/extended-mode.css`, `assets/js/extended-mode.js`, `extended.html`.  
Исправление: не выполнено; сначала завершить первичный прогон переключений и сохранения.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: существующие проверки DOM не проверяют вычисленную видимость.

## MOT-QA-042

ID: MOT-QA-042  
Серьёзность: CRITICAL  
Страница: `extended.html`  
Компонент: «Сохранить черновик» → «Загрузить черновик» / поля общего ввода  
Предусловия: общий режим; комиссия 400 000 ₽, один агент, четыре сделки, расходы 100 000 ₽; итог 62 000 ₽.  
Шаги воспроизведения:
1. Нажать «Сохранить черновик».
2. Изменить видимое поле комиссии с 400 000 ₽ на 100 ₽.
3. Нажать «Загрузить черновик».
4. Сравнить видимое поле и итог.

Ожидаемый результат: поле восстанавливается до 400 000 ₽, а итог остаётся 62 000 ₽.  
Фактический результат: сообщение говорит `Черновик загружен`, итог действительно возвращается к 62 000 ₽, но поле продолжает показывать `100`. На экране одновременно находятся два противоречащих набора данных.  
Повторяемость: 1/1.  
Скриншот: `.playwright-cli/page-2026-07-27T10-39-51-468Z.yml` содержит поле `100`, сообщение `Черновик загружен` и итог `62 000 ₽`.  
Консоль: только общий дефект `favicon.ico` 404.  
Затронутые данные: пользователь не видит фактически загруженные входные значения и может принять итог за расчёт по отображаемым полям.  
Предполагаемая причина: `loadDraft()` заменяет объект `state` и вызывает `render()`, но `render()` не записывает `state.quick` обратно в поля `[data-quick-field]`.  
Файлы: `assets/js/extended-mode.js`, `extended.html`.  
Исправление: не выполнено; восстановление всех полей нужно проверять после загрузки и перезагрузки.  
Повторная проверка: НЕ ВЫПОЛНЕНА — фаза A.  
Регрессионные тесты: существующего браузерного теста восстановления видимых полей нет.
